# Bài 4 — EC2 (Elastic Compute Cloud)

## 1. Mục tiêu
Sau bài này bạn có thể:
- Chọn đúng **instance family + size** cho từng workload.
- Phân biệt 4+ **pricing model** (On-Demand, Reserved, Savings Plans, Spot, Dedicated).
- Phân biệt **EBS vs Instance Store vs EFS**.
- Hiểu **AMI**, **User Data**, **Metadata Service (IMDSv2)**.
- Biết khi nào dùng EC2 vs Lambda vs Fargate.
- Bảo mật EC2 đúng cách (SG, IAM Role thay access key, IMDSv2).

---

## 2. Lý thuyết

### 2.0 Analogy — EC2 như thuê xe ô tô

| Khái niệm EC2 | Trong cho thuê xe | Ý nghĩa |
|----------------|---------------------|---------|
| **Instance type** (`t3.micro`, `m6i.large`) | Hãng + dòng xe (Toyota Vios, Mercedes E-class) | Quyết định performance + giá. |
| **Instance family** (T/M/C/R/G) | Loại xe (sedan / SUV / van / xe đua) | Chọn theo use case (chở người / hàng / đua). |
| **Instance size** (.nano / .micro / .large) | Kích cỡ động cơ (1.5L / 2.0L / 3.0L) | Cùng dòng, sức mạnh khác. |
| **AMI** | Template trang trí xe có sẵn | Linux pre-install, hoặc xe custom của bạn. |
| **Pricing model** | | |
| - On-Demand | Thuê theo giờ taxi | Linh hoạt, đắt nhất. |
| - Reserved (RI) 1y / 3y | Hợp đồng thuê dài hạn trả trước | Cam kết → giảm 40-72%. |
| - Savings Plans | Trả "gói" $X/giờ trong 1-3 năm | Như RI nhưng linh hoạt đổi xe. |
| - Spot | Chỗ trống xe Grab Now | Rẻ 90% nhưng có thể bị huỷ chuyến bất ngờ. |
| - Dedicated Host | Thuê nguyên gara riêng | Compliance, BYOL (Windows Server license). |
| **EBS** | Cốp xe (gắn theo xe) | Lưu hành lý qua chuyến (qua stop/start). |
| **Instance Store** | Đồ trong ghế (xuống xe là mất) | Tốc độ NVMe cực nhanh nhưng mất khi stop. |
| **EFS** | Tủ đồ chung cho cả công ty | Multi-EC2, multi-AZ chia sẻ. |
| **Security Group** | Bảo vệ ngoài gara (firewall) | Stateful, chỉ Allow. |
| **IAM Role** | Bằng lái xe được cấp khi thuê | Không cần mang theo "key" của bạn. |
| **User Data** | Lệnh "chuẩn bị xe trước khi giao" | Cài software lúc launch. |
| **IMDSv2** | Giấy tờ xe có mã PIN | Phòng ai đó "rút" trộm. |

---

### 2.0.1 Câu chuyện — 1 startup, $14,000 bill 1 đêm vì cấu hình EC2 sai

**Tình huống**: Startup Acme deploy app trên EC2. Dev mới vào dùng IAM User cá nhân, commit access key lên public GitHub repo "để test workflow". Trong 8 phút, bot crawler GitHub đã đánh cắp key.

#### Sai cách
1. Launch **20 EC2 p3.16xlarge** (`p3.16xlarge` = $24/giờ × 20 = **$480/giờ**) ở 5 region khác nhau.
2. AMI custom có sẵn miner Monero.
3. Disable CloudTrail logging.
4. Mở SG `0.0.0.0/0` cho mọi port.
5. Sau 8 tiếng → bill $4,000 cho compute + $10,000 cho data transfer cross-region.

→ Sáng hôm sau founder mở email AWS Billing: **$14,000**. Phải gọi AWS Support, may mắn được refund 80% (lần đầu tiên).

#### Đúng cách
1. **Không commit access key** — dùng `git-secrets` pre-commit hook, GitHub secret scanning.
2. **IAM Role** thay access key cho EC2.
3. **Service Control Policy (SCP)**: chặn region không dùng, chặn family `p3/p4/g5` cho account dev (chỉ cho t/m/c size nhỏ).
4. **AWS Budgets** alert $50, $100 → SNS email + auto-stop EC2 (Lambda).
5. **CloudTrail org-wide** + Object Lock S3 → hacker không xoá log được.
6. **Service Quotas** giảm vCPU limit về số thực sự cần (default 16-32 vCPU mỗi region, có thể giảm xuống 8).
7. **GuardDuty** bật → detect crypto-mining trong vài phút.
8. **IMDSv2 required** + HopLimit=1 → SSRF không lấy được role credential.

→ Bị cùng tình huống, **max thiệt hại $50** (budget alert dừng kịp), không có miner chạy được.

(Đây là **kịch bản rất phổ biến** — search "AWS bill shock crypto mining" có hàng nghìn case, có người mất $50k - $200k.)

---

### 2.0.2 Use case map — EC2 vs Lambda vs Fargate vs Lightsail

| Tình huống | Nên chọn | Lý do |
|------------|----------|-------|
| Web API traffic ổn định 24/7 | **EC2 + RI/SP** | Predictable cost, full control. |
| Web API traffic burst, ngủ 80% thời gian | **Lambda + API Gateway** | Trả per-request, scale tự động. |
| Background job batch xử lý 8h/đêm | **EC2 Spot** | Rẻ 90%, chịu interrupt được. |
| Containerized microservice không muốn quản node | **ECS / EKS on Fargate** | Serverless container. |
| Containerized + cần SSH debug + control kernel | **ECS / EKS on EC2** | Có node thật để vào. |
| Cần GPU train ML | **EC2 p4d/p5** + Spot | GPU đắt, Spot tiết kiệm. |
| Cần GPU inference nhỏ thường xuyên | **SageMaker Endpoint** hoặc **Inferentia (Inf2)** | Optimized cho inference. |
| WordPress / Magento self-hosted nhỏ | **Lightsail** | EC2 đã bundle với LB, DB, IP tĩnh — giá phẳng $5/tháng. |
| Game server stateful, low-latency tới user metro | **EC2 + Local Zone** | Latency single-digit ms. |
| App on-prem cũ cần lift-and-shift | **EC2** | Giữ nguyên kiến trúc trước, refactor sau. |
| Workload chạy 1 lần/tháng vài phút | **Lambda** | Free Tier 1M request/tháng. |
| HPC cluster cần MPI low-latency | **EC2 hpc7a + Cluster Placement Group + EFA** | Optimized cho HPC. |
| Workload compliance phải single-tenant hardware | **Dedicated Host** | Hardware riêng, BYOL được. |

---

### 2.0.3 5 hiểu lầm phổ biến về EC2

1. **"t3.micro luôn miễn phí Free Tier"** — SAI một phần. Free Tier cho **750 giờ/tháng t2.micro hoặc t3.micro trong 12 tháng đầu**. Chạy 2 con t3.micro 24/7 = 1440h/tháng → vượt Free Tier → tính tiền. Free Tier không reset mỗi tháng, hết là hết.

2. **"Stop EC2 = không tốn tiền"** — SAI. Compute không tốn, nhưng **EBS volume vẫn tính** ($0.10/GB/tháng cho gp3). 100GB stop quên đó 1 năm = $120. **Elastic IP** không gắn vào running EC2 cũng tính $0.005/giờ ($3.6/tháng).

3. **"Reboot làm mất data Instance Store"** — SAI. **Reboot giữ Instance Store data** (cùng host vật lý). Chỉ **Stop/Start hoặc Terminate** mới mất (đổi host). Đây là bẫy thường gặp.

4. **"Spot Instance bị reclaim ngẫu nhiên không báo trước"** — SAI. AWS gửi **2-phút warning** qua IMDS endpoint `/latest/meta-data/spot/instance-action`. Code phải poll endpoint này (hoặc lắng nghe **Spot Instance Interruption Notice** qua CloudWatch Event/EventBridge) để gracefully shutdown.

5. **"IAM Role gắn vào EC2 thì EC2 luôn dùng quyền của Role"** — SAI một phần. EC2 lấy temp credential từ IMDS. Nếu app **hardcode access key của user** trong env var hoặc `.aws/credentials`, SDK sẽ dùng key đó (precedence cao hơn IMDS). **Best practice**: không set bất kỳ AWS env var nào trên EC2, để SDK tự dùng IMDS.

---

### 2.1 EC2 là gì
EC2 = **virtual server** (VM) chạy trên hạ tầng AWS. Bạn chọn:
- **CPU/RAM/Network** (instance type)
- **OS image** (AMI: Linux, Windows, custom)
- **Storage** (EBS/Instance Store)
- **Network** (VPC, subnet, Security Group)
- **Pricing model**

Tính theo **giây/giờ** (Linux: per-second, min 60s; Windows/RHEL: per-hour).

### 2.2 Instance families & naming
Format: `<family><generation>.<size>` — ví dụ `m6i.large`, `c7g.xlarge`, `t3.micro`.

| Family | Mục đích | Ví dụ |
|--------|----------|-------|
| **T** | Burstable, baseline + credit | `t3.micro`, `t4g.nano` — dev/test, web nhỏ |
| **M** | General-purpose, balanced | `m6i.large`, `m7g.xlarge` — app server |
| **C** | Compute-optimized (CPU mạnh) | `c7i.large` — batch, encoding, HPC |
| **R** | Memory-optimized (RAM nhiều) | `r6i.xlarge` — DB, cache, in-memory |
| **X/Z** | Extreme memory | `x2idn.16xlarge` — SAP HANA |
| **I/D** | Storage-optimized (Instance Store NVMe) | `i4i.large` — NoSQL, data warehouse |
| **G/P/Inf/Trn** | GPU / ML accelerator | `g5.xlarge`, `p4d.24xlarge` — train/inference |
| **HPC** | HPC workload | `hpc7a.96xlarge` |

**Hậu tố hay gặp:**
- `g` — Graviton (ARM, **rẻ hơn 20–40%**, hiệu năng tốt). VD: `m7g`, `t4g`.
- `i` — Intel.
- `a` — AMD.
- `n` — Network optimized (100 Gbps).
- `d` — có Instance Store NVMe.
- `e` — extra memory hoặc enhanced.

→ **Lưu ý đi làm:** Nếu app build được trên ARM (Go/Java/Python/Node), **chuyển sang Graviton là quick win** giảm 20–40% cost cho cùng performance.

### 2.3 T family burstable — cơ chế "credit"
- T-instance có **baseline CPU** (ví dụ `t3.micro` = 10% / 2 vCPU).
- Khi CPU dưới baseline → tích **CPU credit**.
- Khi CPU vượt baseline → **tiêu credit**.
- Hết credit:
  - **Standard mode**: throttle về baseline (chậm lại).
  - **Unlimited mode** (default từ `t3`): vẫn burst, **trả thêm phí** theo vCPU-hour.
- ⚠️ Bẫy đi làm: T-unlimited có thể **vọt cost** nếu app load liên tục cao → nên dùng M family.

### 2.4 AMI (Amazon Machine Image)
- Template OS + pre-installed software → launch nhanh.
- Có 4 nguồn:
  - **AWS-provided**: Amazon Linux 2023, Ubuntu, Windows Server.
  - **Marketplace**: hình ảnh từ vendor (Bitnami, Palo Alto…), có thể tính phí thêm.
  - **Community**: AMI public.
  - **My AMIs**: bạn tự build (ví dụ qua **Packer** hoặc **EC2 Image Builder**).
- AMI **regional** — copy sang region khác phải `aws ec2 copy-image`.
- AMI có 2 backing storage:
  - **EBS-backed** (phổ biến): root volume là EBS, có thể stop/start.
  - **Instance Store-backed** (hiếm): không stop được, mất data khi terminate.

### 2.5 Storage cho EC2

| | EBS | Instance Store | EFS | FSx |
|--|-----|----------------|-----|-----|
| Loại | Block, network-attached | Block, **physical-attached** | NFS file system | Windows SMB / Lustre / NetApp / OpenZFS |
| Persistence | **Survives stop/terminate** | **Mất khi stop/terminate** | Persistent | Persistent |
| AZ | 1 AZ (gắn EC2 cùng AZ) | Same host | **Multi-AZ** | Multi-AZ tuỳ loại |
| Share multi-EC2 | Không (trừ io1/io2 Multi-Attach same AZ) | Không | **Có** (1000s EC2) | Có |
| IOPS/Throughput | Tuỳ type, max 256k IOPS (io2 BX) | **Cực cao**, NVMe local | Auto scale | Lustre cực cao |
| Cost | $/GB/tháng + IOPS phí | Đi kèm instance type | $/GB sử dụng | Cao |
| Snapshot | Có (vào S3) | Không | AWS Backup | Có |
| Use case | Boot, DB, app | Cache, scratch, temp | Shared content, lift-and-shift NFS | Windows file share, HPC |

**EBS volume types:**
| Type | Use case | IOPS | Note |
|------|----------|------|------|
| **gp3** | Default mới | 3000–16000 base, tách size & IOPS | Rẻ hơn gp2 ~20% |
| **gp2** | Default cũ | IOPS = 3 × size GB | Vẫn dùng nhiều, sẽ migrate gp3 dần |
| **io1 / io2** | DB perf cao | Tới 64k (io2 BX: 256k) | Có Multi-Attach (16 EC2 same AZ) |
| **io2 Block Express** | Extreme | 256k IOPS, 4000 MB/s | Nitro only |
| **st1** | Throughput HDD | Big data, log | Không boot được |
| **sc1** | Cold HDD | Backup, archive | Rẻ nhất block |

### 2.6 Pricing models (cực hay ra đề)

| Model | Discount | Cam kết | Use case |
|-------|----------|---------|----------|
| **On-Demand** | 0% | Không | Dev/test, workload không dự đoán |
| **Reserved Instance (RI)** | Tới 72% | 1 hoặc 3 năm, instance family + region | Steady state workload |
| **Savings Plans (Compute)** | Tới 66% | 1/3 năm, commit $/h, **flexible** EC2 + Fargate + Lambda | Workload thay đổi type/region |
| **Savings Plans (EC2 Instance)** | Tới 72% | 1/3 năm, fix family + region | Như RI nhưng size flexible |
| **Spot Instance** | Tới 90% | Không, có thể bị kill trong 2 phút | Stateless, batch, CI, fault-tolerant |
| **Dedicated Host** | Đắt nhất | 1/3 năm hoặc on-demand | License BYOL (Windows, Oracle) — visibility tới socket/core |
| **Dedicated Instance** | Đắt | Per hour | Không share HW với account khác |
| **Capacity Reservation** | Không discount | Per hour | Đảm bảo capacity khi bạn cần |

**Khi nào dùng gì:**
- Prod web 24/7 ổn định → **Savings Plan 1 năm no-upfront**.
- Batch job stateless, có thể retry → **Spot Fleet capacity-optimized**.
- Workload spike khó dự đoán → **On-Demand + Auto Scaling**.
- Cần đảm bảo capacity trong DR region → **Capacity Reservation**.
- License Windows/Oracle tính theo socket → **Dedicated Host**.

### 2.7 Lifecycle states
- **Pending** → instance đang khởi tạo.
- **Running** → có phí compute + EBS.
- **Stopping/Stopped** → **không tính phí compute**, vẫn tính phí EBS, mất Instance Store data, **có thể** đổi instance type.
- **Hibernate** → lưu RAM xuống EBS root, start lại nhanh. Yêu cầu encrypted EBS, certain types.
- **Terminating/Terminated** → xóa instance. EBS root volume xóa theo (default), trừ khi unset `DeleteOnTermination`.
- **Reboot** ≠ Stop/Start — reboot giữ public IP, không thay đổi host.

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 430" role="img" style="width:100%;max-width:720px;height:auto;display:block;margin:1.25rem auto" font-family="ui-sans-serif, system-ui, sans-serif">
  <title>Vòng đời trạng thái của EC2 instance</title>
  <desc>Máy trạng thái EC2: Pending khởi tạo rồi sang Running (tính phí compute). Từ Running có thể Reboot (giữ host và Instance Store), Stop sang Stopping rồi Stopped (ngừng phí compute, mất Instance Store, có thể đổi host khi Start lại), Hibernate (lưu RAM xuống EBS), hoặc Terminate sang Terminated (xóa vĩnh viễn, mất Instance Store).</desc>
  <defs>
    <marker id="ec2Arr" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto"><path d="M0 0 L8 3 L0 6 z" fill="currentColor" fill-opacity="0.55"/></marker>
  </defs>
  <text x="16" y="24" font-size="14" font-weight="700" fill="currentColor">Vòng đời EC2 instance</text>

  <!-- Pending -->
  <rect x="20" y="56" width="150" height="56" rx="10" fill="#f59e0b" fill-opacity="0.15" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="95" y="80" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">Pending</text>
  <text x="95" y="98" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">đang khởi tạo</text>

  <!-- Running -->
  <rect x="285" y="56" width="160" height="56" rx="10" fill="#10b981" fill-opacity="0.16" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="365" y="80" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">Running</text>
  <text x="365" y="98" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">phí compute + EBS</text>

  <!-- Stopping -->
  <rect x="285" y="178" width="160" height="50" rx="10" fill="#3b82f6" fill-opacity="0.14" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="365" y="208" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">Stopping</text>

  <!-- Stopped -->
  <rect x="285" y="290" width="160" height="56" rx="10" fill="#3b82f6" fill-opacity="0.14" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="365" y="314" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">Stopped</text>
  <text x="365" y="332" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">0 phí compute · còn EBS</text>

  <!-- Terminated -->
  <rect x="540" y="290" width="160" height="56" rx="10" fill="#8b5cf6" fill-opacity="0.15" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="620" y="314" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">Terminated</text>
  <text x="620" y="332" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">xóa vĩnh viễn</text>

  <!-- Pending -> Running -->
  <line x1="170" y1="84" x2="281" y2="84" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>

  <!-- Running -> Stopping (Stop / Hibernate) -->
  <line x1="350" y1="112" x2="350" y2="174" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>
  <text x="342" y="148" font-size="10.5" text-anchor="end" fill="currentColor" opacity="0.85">Stop / Hibernate</text>

  <!-- Stopping -> Stopped -->
  <line x1="365" y1="228" x2="365" y2="286" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>

  <!-- Stopped -> Running (Start) -->
  <path d="M445 304 C 520 250, 520 130, 449 90" fill="none" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>
  <text x="512" y="200" font-size="10.5" text-anchor="middle" fill="currentColor" opacity="0.85">Start</text>
  <text x="512" y="216" font-size="9.5" text-anchor="middle" fill="currentColor" opacity="0.6">(có thể đổi host)</text>

  <!-- Stopped -> Terminated -->
  <line x1="445" y1="318" x2="536" y2="318" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>
  <text x="490" y="310" font-size="10.5" text-anchor="middle" fill="currentColor" opacity="0.85">Terminate</text>

  <!-- Running -> Terminated -->
  <path d="M445 100 C 600 130, 620 220, 620 286" fill="none" stroke="currentColor" stroke-opacity="0.5" marker-end="url(#ec2Arr)"/>
  <text x="600" y="170" font-size="10.5" text-anchor="middle" fill="currentColor" opacity="0.85">Terminate</text>

  <!-- Reboot self-loop -->
  <path d="M420 56 C 440 24, 480 30, 460 58" fill="none" stroke="#10b981" stroke-opacity="0.7" marker-end="url(#ec2Arr)"/>
  <text x="488" y="40" font-size="10.5" fill="currentColor" opacity="0.85">Reboot</text>

  <!-- Legend / Instance Store notes -->
  <rect x="20" y="360" width="680" height="58" rx="9" fill="currentColor" fill-opacity="0.05" stroke="currentColor" stroke-opacity="0.18"/>
  <text x="32" y="380" font-size="11" font-weight="700" fill="currentColor">Instance Store (ổ ephemeral):</text>
  <text x="32" y="398" font-size="10.5" fill="currentColor" opacity="0.8">Reboot → GIỮ host, GIỮ dữ liệu Instance Store.   Stop/Hibernate/Terminate → MẤT Instance Store.</text>
  <text x="32" y="413" font-size="10.5" fill="currentColor" opacity="0.8">Hibernate = lưu RAM xuống EBS root (cần EBS mã hoá); Terminate xóa EBS root nếu DeleteOnTermination=true.</text>
</svg>

### 2.8 User Data & Metadata

**User Data**: script chạy **lần đầu boot** (root, /var/log/cloud-init-output.log). Ví dụ:
```bash
#!/bin/bash
yum update -y
yum install -y nginx
systemctl enable --now nginx
echo "<h1>Hello from $(hostname)</h1>" > /usr/share/nginx/html/index.html
```

**Instance Metadata Service (IMDS)**: HTTP endpoint **`http://169.254.169.254/latest/meta-data/`** chỉ accessible từ chính EC2.

⚠️ **IMDSv2 vs v1**:
- **IMDSv1** (legacy): query trực tiếp → bị **SSRF** lợi dụng (Capital One 2019 leak 100M user).
- **IMDSv2**: bắt buộc **session token** (PUT request lấy token trước, GET kèm header).
- **Best practice 2024+**: bắt buộc IMDSv2-only ở launch template, **`HttpTokens=required`**.

Code thường dùng:
```bash
# Lấy token (TTL 6h)
TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" \
  -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

# Dùng token query metadata
curl -s -H "X-aws-ec2-metadata-token: $TOKEN" \
  http://169.254.169.254/latest/meta-data/iam/security-credentials/
```

### 2.9 Security Group (SG) recap
- **Stateful**, instance-level (gắn vào ENI).
- Chỉ rule **Allow**, không có Deny.
- Inbound default: **deny all**. Outbound default: **allow all**.
- Source/destination: CIDR, **SG khác** (self-reference được), prefix list.
- Có thể gắn **nhiều SG** vào 1 ENI — rules **union**.

Best practice:
- SG `web-sg` allow `0.0.0.0/0:443`.
- SG `app-sg` allow `web-sg:8080` (chain by reference, không bằng CIDR cứng).
- SG `db-sg` allow `app-sg:5432`.

<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 320" role="img" style="width:100%;max-width:720px;height:auto;display:block;margin:1.25rem auto" font-family="ui-sans-serif, system-ui, sans-serif">
  <title>Chaining Security Group bằng tham chiếu SG thay vì CIDR</title>
  <desc>Internet vào web-sg cho phép 0.0.0.0/0 cổng 443. web-sg tham chiếu vào app-sg cổng 8080, app-sg tham chiếu vào db-sg cổng 5432. Mỗi Security Group là stateful nên response trả về tự động được phép, không cần outbound rule riêng. Source là chính SG đứng trước, không phải dải CIDR cứng.</desc>
  <defs>
    <marker id="sgArr" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto"><path d="M0 0 L8 3 L0 6 z" fill="currentColor" fill-opacity="0.6"/></marker>
  </defs>
  <text x="16" y="24" font-size="14" font-weight="700" fill="currentColor">Security Group chaining bằng reference</text>

  <!-- Internet -->
  <rect x="16" y="70" width="120" height="60" rx="10" fill="currentColor" fill-opacity="0.06" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="76" y="96" font-size="12.5" font-weight="700" text-anchor="middle" fill="currentColor">Internet</text>
  <text x="76" y="114" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">0.0.0.0/0</text>

  <!-- web-sg -->
  <rect x="196" y="64" width="140" height="72" rx="10" fill="#3b82f6" fill-opacity="0.14" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="266" y="90" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">web-sg</text>
  <text x="266" y="108" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">tier web (ALB/EC2)</text>
  <text x="266" y="124" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">in: 443</text>

  <!-- app-sg -->
  <rect x="396" y="64" width="140" height="72" rx="10" fill="#10b981" fill-opacity="0.16" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="466" y="90" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">app-sg</text>
  <text x="466" y="108" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">tier app</text>
  <text x="466" y="124" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">in: 8080</text>

  <!-- db-sg -->
  <rect x="580" y="64" width="124" height="72" rx="10" fill="#8b5cf6" fill-opacity="0.15" stroke="currentColor" stroke-opacity="0.25"/>
  <text x="642" y="90" font-size="13" font-weight="700" text-anchor="middle" fill="currentColor">db-sg</text>
  <text x="642" y="108" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">tier DB</text>
  <text x="642" y="124" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">in: 5432</text>

  <!-- edges -->
  <line x1="136" y1="100" x2="192" y2="100" stroke="currentColor" stroke-opacity="0.55" marker-end="url(#sgArr)"/>
  <text x="164" y="92" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.85">443</text>

  <line x1="336" y1="100" x2="392" y2="100" stroke="currentColor" stroke-opacity="0.55" marker-end="url(#sgArr)"/>
  <text x="364" y="92" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.85">8080</text>

  <line x1="536" y1="100" x2="576" y2="100" stroke="currentColor" stroke-opacity="0.55" marker-end="url(#sgArr)"/>
  <text x="556" y="92" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.85">5432</text>

  <!-- source = SG references -->
  <text x="266" y="170" font-size="10" text-anchor="middle" fill="currentColor" opacity="0.7">source: 0.0.0.0/0</text>
  <text x="466" y="170" font-size="10.5" text-anchor="middle" fill="currentColor" opacity="0.85">source: <tspan font-weight="700">web-sg</tspan></text>
  <text x="642" y="170" font-size="10.5" text-anchor="middle" fill="currentColor" opacity="0.85">source: <tspan font-weight="700">app-sg</tspan></text>
  <line x1="266" y1="178" x2="266" y2="136" stroke="currentColor" stroke-opacity="0.3" stroke-dasharray="3 3"/>
  <line x1="466" y1="178" x2="466" y2="136" stroke="currentColor" stroke-opacity="0.3" stroke-dasharray="3 3"/>
  <line x1="642" y1="178" x2="642" y2="136" stroke="currentColor" stroke-opacity="0.3" stroke-dasharray="3 3"/>

  <!-- stateful note -->
  <rect x="16" y="206" width="688" height="44" rx="9" fill="#10b981" fill-opacity="0.1" stroke="currentColor" stroke-opacity="0.18"/>
  <text x="28" y="226" font-size="11" font-weight="700" fill="currentColor">Stateful:</text>
  <text x="96" y="226" font-size="10.5" fill="currentColor" opacity="0.85">request được allow → response trả về TỰ ĐỘNG được phép (không cần outbound rule tương ứng).</text>
  <text x="28" y="242" font-size="10.5" fill="currentColor" opacity="0.85">Mở rộng tier (thêm EC2 vào app-sg) không phải sửa rule db-sg — tham chiếu SG, không hardcode IP.</text>

  <!-- bad practice contrast -->
  <rect x="16" y="262" width="688" height="42" rx="9" fill="#f59e0b" fill-opacity="0.12" stroke="currentColor" stroke-opacity="0.18"/>
  <text x="28" y="282" font-size="11" font-weight="700" fill="currentColor">Tránh:</text>
  <text x="90" y="282" font-size="10.5" fill="currentColor" opacity="0.85">db-sg allow 10.0.1.0/24:5432 — CIDR cứng vỡ khi đổi subnet/scale, không biểu đạt "ai" được nối.</text>
  <text x="28" y="298" font-size="10.5" fill="currentColor" opacity="0.85">Reference SG = ý định rõ ràng (app tier → db tier) + tự co giãn theo instance.</text>
</svg>

### 2.10 Khi nào dùng EC2 vs các option khác

| Workload | Service đúng |
|----------|--------------|
| Code Node/Python ngắn (<15 phút), event-driven | **Lambda** |
| Container không muốn quản EC2, scale to zero | **App Runner** hoặc **ECS Fargate** |
| Container orchestration, customize sâu | **ECS on EC2** / **EKS** |
| Web app traditional, full OS control, GPU, legacy | **EC2** |
| HPC cluster, MPI, low-latency | **EC2 với Placement Group cluster + EFA** |
| Database self-managed | EC2 — nhưng cân nhắc RDS/Aurora trước |
| Batch job có thể bị interrupt | **EC2 Spot** hoặc **AWS Batch** |

### 2.11 Auto Scaling Group (giới thiệu, đi sâu ở SAA)
- **Launch Template** (mới) thay **Launch Configuration** (cũ).
- Min / Desired / Max capacity.
- Health check: EC2 hoặc ELB.
- Scaling policies: Target Tracking, Step, Scheduled, Predictive.
- Multi-AZ → 1 instance/AZ tự balance.

### 2.12 Placement Groups
- **Cluster** — same AZ, low latency, HPC. Risk: AZ down = mất tất.
- **Spread** — 7 instance/AZ, different HW. Đảm bảo isolation.
- **Partition** — up to 7 partition/AZ, HDFS/Kafka.

### 2.13 Elastic IP (EIP)
- **Static public IPv4** thuộc về account, attach vào ENI.
- **Phí khi KHÔNG dùng** (~$3.6/tháng/EIP idle, từ 2024 cả khi đang dùng cũng tính phí $0.005/h).
- Tránh dùng nếu có thể → ưu tiên ALB/NLB hoặc Route 53 alias.

---

## 3. Hands-on có account

### Lab 1 — Launch EC2 đầu tiên (15 phút, FREE TIER)
```bash
# 1. Tạo key pair
aws ec2 create-key-pair --key-name learn-key \
  --query 'KeyMaterial' --output text > ~/.ssh/learn-key.pem
chmod 400 ~/.ssh/learn-key.pem

# 2. Lấy AMI ID Amazon Linux 2023 mới nhất
AMI=$(aws ec2 describe-images --owners amazon \
  --filters "Name=name,Values=al2023-ami-*-x86_64" "Name=state,Values=available" \
  --query 'sort_by(Images, &CreationDate)[-1].ImageId' --output text)

# 3. Tạo SG cho phép SSH từ IP của bạn
MY_IP=$(curl -s ifconfig.me)
SG=$(aws ec2 create-security-group --group-name learn-ssh \
  --description "Learn SSH" --query 'GroupId' --output text)
aws ec2 authorize-security-group-ingress --group-id $SG \
  --protocol tcp --port 22 --cidr ${MY_IP}/32

# 4. Launch t3.micro với IMDSv2 required
aws ec2 run-instances --image-id $AMI --instance-type t3.micro \
  --key-name learn-key --security-group-ids $SG \
  --metadata-options "HttpTokens=required,HttpPutResponseHopLimit=2" \
  --tag-specifications 'ResourceType=instance,Tags=[{Key=Name,Value=learn-ec2-1},{Key=Project,Value=aws-learner}]'

# 5. SSH vào
aws ec2 describe-instances --filters "Name=tag:Name,Values=learn-ec2-1" \
  --query 'Reservations[].Instances[].PublicIpAddress' --output text
ssh -i ~/.ssh/learn-key.pem ec2-user@<PUBLIC_IP>
```

Trên EC2:
```bash
# Lấy metadata (IMDSv2)
TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" \
  -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")
curl -H "X-aws-ec2-metadata-token: $TOKEN" \
  http://169.254.169.254/latest/meta-data/instance-id
```

**Teardown:**
```bash
INSTANCE=$(aws ec2 describe-instances --filters "Name=tag:Name,Values=learn-ec2-1" \
  --query 'Reservations[].Instances[].InstanceId' --output text)
aws ec2 terminate-instances --instance-ids $INSTANCE
aws ec2 delete-security-group --group-id $SG  # chờ instance terminate xong
aws ec2 delete-key-pair --key-name learn-key
```

### Lab 2 — User Data tự cài web server (10 phút)
```bash
cat > /tmp/user-data.sh <<'EOF'
#!/bin/bash
dnf install -y nginx
systemctl enable --now nginx
TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")
IID=$(curl -s -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/instance-id)
echo "<h1>Hello from $IID</h1>" > /usr/share/nginx/html/index.html
EOF

aws ec2 run-instances ... --user-data file:///tmp/user-data.sh \
  --security-group-ids $SG_WITH_PORT_80 ...
```

Mở SG port 80, curl public IP → thấy "Hello from i-xxx".

### Lab 3 — Stop/Start: thay đổi instance type (5 phút)
```bash
aws ec2 stop-instances --instance-ids $INSTANCE
aws ec2 modify-instance-attribute --instance-id $INSTANCE --instance-type "{\"Value\": \"t3.small\"}"
aws ec2 start-instances --instance-ids $INSTANCE
```

Lưu ý public IP **đổi** sau stop/start (trừ khi gắn EIP).

### Lab 4 — IAM Role thay access key (15 phút)
```bash
# Tạo role + instance profile (làm 1 lần)
aws iam create-role --role-name learn-ec2-s3 \
  --assume-role-policy-document '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"ec2.amazonaws.com"},"Action":"sts:AssumeRole"}]}'
aws iam attach-role-policy --role-name learn-ec2-s3 \
  --policy-arn arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
aws iam create-instance-profile --instance-profile-name learn-ec2-s3-profile
aws iam add-role-to-instance-profile \
  --instance-profile-name learn-ec2-s3-profile --role-name learn-ec2-s3

# Launch EC2 với profile
aws ec2 run-instances ... --iam-instance-profile Name=learn-ec2-s3-profile
```

SSH vào, chạy `aws s3 ls` — không cần `aws configure`. SDK tự lấy temp credential từ IMDS.

### Lab 5 — Spot Instance (10 phút, ~$0.003/h)
```bash
aws ec2 run-instances --image-id $AMI --instance-type t3.micro \
  --instance-market-options 'MarketType=spot,SpotOptions={MaxPrice=0.01,SpotInstanceType=one-time}'
```

Xem giá Spot history:
```bash
aws ec2 describe-spot-price-history --instance-types t3.micro \
  --product-descriptions "Linux/UNIX" --max-items 5
```

---

## 4. Hands-on không tốn tiền

### LocalStack EC2 (có hạn chế — không boot OS thật, nhưng test được API)
```bash
awslocal ec2 describe-images --owners amazon
awslocal ec2 run-instances --image-id ami-12345 --instance-type t3.micro
awslocal ec2 describe-instances
```

### Multipass / UTM (no-cost VM local)
Mô phỏng EC2 trên máy bạn:
```bash
brew install multipass
multipass launch --name learn-ec2 --cpus 1 --memory 1G --disk 8G
multipass shell learn-ec2
# bên trong: install nginx như user-data
```

### EC2 Instance Type Comparator
- https://instances.vantage.sh/ — so sánh giá + spec mọi EC2 type, 4 region, 4 pricing model.
- Bài tập: tìm instance **rẻ nhất** thoả mãn 4 vCPU + 16GB RAM ở `ap-southeast-1`.

### Pricing exercise (giấy)
1 app chạy 24/7, 4 vCPU, 16GB RAM, ổn định 3 năm. So sánh cost:
- On-Demand `m6i.xlarge`
- RI `m6i.xlarge` 3-year all-upfront
- Compute Savings Plan 3-year no-upfront
- Spot

→ Đáp án trong practice solution.

---

## 5. Tự kiểm tra

1. App của bạn chạy ổn định 24/7, không đổi region/family trong 3 năm. Pricing nào tối ưu?
   <details><summary>Đáp án</summary>**EC2 Instance Savings Plan 3-year all-upfront** (~72% off). RI cũng OK nhưng SP linh hoạt size hơn.</details>

2. Bạn cần shared file system cho 50 EC2 Linux ở 3 AZ. Chọn gì?
   <details><summary>Đáp án</summary>**EFS** — multi-AZ NFS. EBS không share multi-AZ (Multi-Attach chỉ same AZ và max 16 instance).</details>

3. Spot instance bị reclaim, bạn có bao nhiêu thời gian xử lý?
   <details><summary>Đáp án</summary>**2 phút** notification qua IMDS `/latest/meta-data/spot/instance-action`. Code phải graceful shutdown trong 2 phút.</details>

4. EC2 stop/start có giữ data trên Instance Store không?
   <details><summary>Đáp án</summary>**KHÔNG** — Instance Store mất khi stop hoặc terminate (chỉ giữ khi reboot). EBS thì giữ.</details>

5. Web app cần protect khỏi SSRF. EC2 cấu hình thế nào?
   <details><summary>Đáp án</summary>Bật **IMDSv2 required** (`HttpTokens=required`), set **HopLimit=1** để container/proxy không relay được. Capital One breach 2019 là vì IMDSv1 cho phép SSRF.</details>

6. Bạn cần 100 EC2 chạy song song HPC, latency < 1ms giữa các node. Đặt vào Placement Group nào?
   <details><summary>Đáp án</summary>**Cluster** placement group (same AZ, same rack). Bonus: dùng **EFA (Elastic Fabric Adapter)** cho MPI workload.</details>

7. Reboot khác Stop/Start ở chỗ nào?
   <details><summary>Đáp án</summary>Reboot **giữ host vật lý + public IP + Instance Store data**. Stop/Start **đổi host + đổi public IP (trừ EIP) + mất Instance Store**, có thể đổi instance type.</details>

8. Khi terminate EC2, EBS root volume mặc định bị xóa hay giữ?
   <details><summary>Đáp án</summary>**Xóa** (DeleteOnTermination=true mặc định cho root). Volume gắn thêm thì mặc định **giữ** (false).</details>

---

## 6. Đối chiếu GCP

| Khái niệm | AWS | GCP |
|-----------|-----|-----|
| VM service | **EC2** | **Compute Engine (GCE)** |
| Instance type | `m6i.large`, `c7g.xlarge` | `n2-standard-4`, `c3-highcpu-4` |
| ARM CPU rẻ | **Graviton** (`g` suffix) | **Tau T2A** / **Axion** |
| Image | **AMI** (regional) | **Image** (multi-regional) |
| Boot disk | **EBS gp3** (network block) | **Persistent Disk** (zonal/regional) |
| Local SSD | **Instance Store NVMe** | **Local SSD** |
| Shared NFS | **EFS** | **Filestore** |
| Auto Scaling | **ASG + Launch Template** | **MIG (Managed Instance Group) + Instance Template** |
| Cheap pre-emptible | **Spot Instance** (2 min warning) | **Spot VM** (30s warning) |
| Discount cam kết | **RI / Savings Plans** | **Committed Use Discount (CUD)** |
| Sustained discount tự động | Không có | **Sustained Use Discount** (auto khi chạy > 25% tháng) |
| Static IP | **Elastic IP** | **Static External IP** |
| Metadata | **IMDS** (`169.254.169.254`, có v1/v2) | **Metadata Server** (`metadata.google.internal`, bắt buộc header `Metadata-Flavor: Google`) |
| Firewall | **Security Group** (per-ENI, stateful, allow-only) | **VPC Firewall Rule** (per-network, stateful, có Allow/Deny + priority) |
| Service identity | **IAM Role + Instance Profile** | **Service Account** attach vào instance |

**5 bẫy lớn khi từ GCE qua EC2:**
1. **Persistent Disk regional** không có equivalent ở EBS. Muốn HA storage → **EFS** hoặc **replication app-level**.
2. **GCE auto sustained discount** (free, automatic). AWS phải **chủ động mua** Savings Plan/RI — quên là mất tiền.
3. **GCP metadata server** dùng header `Metadata-Flavor: Google` (không có v2 token). AWS **bắt buộc IMDSv2 token** mới.
4. **GCP Service Account ≈ AWS IAM Role + Instance Profile**, nhưng GCP gắn 1 service account/VM trực tiếp; AWS phải qua Instance Profile (1 wrapper).
5. **Firewall** GCP có **priority + Deny rules**, AWS SG **chỉ Allow, không priority** (Deny ở NACL level). Đổi mindset khi thiết kế.

**Khi đi làm multi-cloud:**
- Pricing comparator: https://instances.vantage.sh/ cho AWS, https://gcloud-compute.com/ cho GCE.
- Image baking: **Packer** chạy được cả 2 (HCL template chung).
- IaC: **Terraform** > native (CloudFormation/Deployment Manager).

---

## 7. Lưu ý khi thi CLF-C02
- Nhớ 4 pricing model + use case: On-Demand, Reserved, Spot, Savings Plans.
- EC2 = IaaS (full control OS).
- **Free Tier**: 750h/tháng `t2.micro` hoặc `t3.micro` Linux/Windows, 12 tháng.
- EBS persistent, Instance Store ephemeral.
- Security Group stateful, NACL stateless.
- Khi thấy "fault-tolerant, batch, cost-saving" → **Spot**.
- Khi thấy "license BYOL Windows/Oracle" → **Dedicated Host**.

## 8. Lưu ý khi thi SAA-C03
- **EBS Multi-Attach** chỉ io1/io2 **same AZ**, max 16 instance, application phải cluster-aware (GFS2/OCFS2).
- **Hibernate** yêu cầu encrypted root, RAM ≤ 150GB, only certain instance families, on-demand/reserved (không spot).
- **Placement Group Cluster** rủi ro 1 AZ down → mất hết. Critical → **Spread**.
- **Capacity Reservation** ≠ Reserved Instance. Capacity Res chỉ đảm bảo HW có sẵn (vẫn trả on-demand), không discount. RI/SP là discount.
- **EC2 Image Builder** pipeline tự động build AMI patched hàng tuần — câu hỏi về CI/CD AMI.
- **ASG Lifecycle Hook** + **Warm Pool** giảm scale-out latency.
- **Termination Policy** trong ASG: OldestInstance / NewestInstance / OldestLaunchTemplate / ClosestToNextInstanceHour / Default.
- **Spot Fleet capacity-optimized** strategy ít bị interrupt nhất (khác price-capacity-optimized hoặc lowest-price).
- **EFA (Elastic Fabric Adapter)** cho HPC/MPI, KHÔNG dùng cho TCP/IP thông thường.
- **Nitro System** instances → support EBS encryption by default, IMDSv2, bare metal, hibernation, performance cao hơn.

## 9. Lưu ý khi đi làm

### Cost
- **Đo trước, tối ưu sau**: chạy 2 tuần on-demand → xem CloudWatch CPU/RAM utilization → right-size → mua Savings Plan.
- **Compute Optimizer** (free): gợi ý down-size instance dựa trên 14 ngày metric.
- **Spot Fleet** mix instance types để giảm interruption rate (`capacity-optimized`).
- **Graviton (ARM)** → 20–40% savings cho workload portable.
- **Schedule stop/start** cho dev/test (Instance Scheduler hoặc Lambda + EventBridge) → save 60% (chỉ chạy 9–6 weekday).
- Tag `Environment`, `Owner`, `CostCenter` ngay từ ngày 1 — cost allocation về sau dễ hơn nhiều.

### Bảo mật
- **IMDSv2 required** ở mọi launch template (account-level enforce qua SCP).
- **SSM Session Manager** thay SSH port 22 → no bastion, no inbound rule, full audit trail.
- **Patch Manager (SSM)** auto-patch hàng tuần.
- **EBS encryption by default** bật ở account level mỗi region.
- **AMI Vending** — security team build AMI hardened, dev chỉ dùng AMI approved.
- **Inspector** scan vulnerability EC2 + ECR.
- Không cho EC2 có **public IP** trừ khi cần. Dùng NAT GW cho egress private.

### Anti-pattern thường gặp
- ❌ Access key hardcode trong user-data hoặc app config → dùng **IAM Role**.
- ❌ SSH 0.0.0.0/0 port 22 → **SSM Session Manager**.
- ❌ Self-managed MySQL trên EC2 → cân nhắc **RDS** trước.
- ❌ Snapshot không cross-region → DR thất bại nếu region down. **AWS Backup** với CRR.
- ❌ AMI 2 năm chưa update → vulnerable. Image Builder pipeline.
- ❌ T-unlimited cho prod load cao liên tục → vọt cost, dùng M family.
- ❌ Elastic IP idle → $3.6/tháng/EIP. Release khi không dùng.

---

## 10. Flashcard

- **EC2** — virtual server, billed per-second (Linux) / per-hour (Windows).
- **AMI** — template OS, regional.
- **Instance type** — `family.size` (m6i.large), Graviton (g) rẻ 20–40%.
- **T burstable** — credit-based, Unlimited mode có thể vọt cost.
- **EBS** — block storage, network-attached, 1 AZ, survives stop/terminate.
- **gp3** — default mới, tách size & IOPS, rẻ hơn gp2 20%.
- **Instance Store** — local NVMe, ephemeral.
- **EFS** — multi-AZ NFS shared.
- **Pricing** — On-Demand / Reserved / Savings Plan / Spot / Dedicated.
- **Spot** — 90% off, 2 min interruption notice.
- **Compute SP** — flexible EC2+Fargate+Lambda. **EC2 SP** — fix family/region, max discount.
- **Capacity Reservation** — reserve HW, không discount.
- **User Data** — script chạy lần đầu boot.
- **IMDS** — `169.254.169.254`, **dùng IMDSv2 (token-based)** chống SSRF.
- **Instance Profile** — container chứa IAM Role gắn vào EC2.
- **Placement Group** — Cluster (HPC) / Spread (isolation) / Partition (HDFS/Kafka).
- **Hibernate** — lưu RAM xuống EBS, encrypted only.
- **EIP** — static public IP, có phí, idle hoặc dùng đều tính (2024+).
- **Nitro** — modern hypervisor, hỗ trợ bare metal, encryption default, IMDSv2, hibernation.
- **Stop/Start** — đổi public IP (trừ EIP), mất Instance Store, có thể change type.
- **Terminate** — xóa instance, root EBS xóa theo default.
